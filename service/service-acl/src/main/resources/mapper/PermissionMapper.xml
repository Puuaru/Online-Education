<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.puuaru.acl.mapper.PermissionMapper">

    <select id="getPermissionsByUserId" resultType="java.lang.String">
        select p.permission_value
        from acl_permission p
                 join acl_role_permission rp on rp.permission_id = p.id
                 join acl_user_role ur on ur.role_id = rp.role_id
        where ur.user_id = #{userId} and p.type = 2 and p.is_deleted = 0
    </select>
</mapper>
